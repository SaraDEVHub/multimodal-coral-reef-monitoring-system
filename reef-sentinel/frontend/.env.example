# URL de base de l'API backend (sans slash final), utilisée uniquement si le
# frontend et le backend ne sont pas servis derrière le même proxy.
# En développement, laisser commenté : le proxy Vite (vite.config.js) suffit.
# VITE_API_BASE_URL=https://api.mon-domaine.com/api
